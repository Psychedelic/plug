import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    padding: '12px 24px',
    alignItems: 'center',
  },
  icon: {
    marginLeft: 'auto',
  },
  title: {
    fontSize: 16,
    color: '#111827',
    fontWeight: 500,
    marginLeft: 12,
  },
});
