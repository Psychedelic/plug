import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  whitelistContainer: {
    display: 'flex',
    width: '100%',
    flexDirection: 'column',
    marginBottom: 24,
    maxHeight: 173,
    overflow: 'auto',
  },
});
