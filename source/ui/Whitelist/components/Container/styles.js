import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  whitelistContainer: {
    display: 'flex',
    width: '100%',
    flexDirection: 'column',
    overflowY: 'auto',
    overflowX: 'hidden',
    maxHeight: '140px',
    margin: '10px 0',
  },
});
