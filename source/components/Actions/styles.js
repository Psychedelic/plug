import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    height: 120,
    width: 300,
    margin: 'auto',
    display: 'flex',
    justifyContent: 'space-evenly',
    alignItems: 'center',
  },
});
