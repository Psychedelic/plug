import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  buttonContainer: {
    width: '100%',
    display: 'flex',
    justifyContent: 'space-between',
  },
  padTop: {
    paddingTop: 35,
  },
});
